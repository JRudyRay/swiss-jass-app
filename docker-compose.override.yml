# Docker Compose override for development
services:
  swiss-jass-backend:
    environment:
      - NODE_ENV=development
    volumes:
      # Mount source code for development
      - ./backend/src:/app/src
    ports:
      - "3000:3000"
    command: ["npm", "run", "dev"]